<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<!-- Permissions -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.INTERNET" />

<!-- Storage permissions for older Android versions -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

<uses-permission
    android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />

<!-- Android 13+ -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />


<application
    android:label="DocScanner"
    android:name="${applicationName}"
    android:icon="@mipmap/ic_launcher">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:theme="@style/LaunchTheme"
        android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
        android:hardwareAccelerated="true"
        android:windowSoftInputMode="adjustResize"
        android:grantUriPermissions="true">

        <!-- Flutter Theme -->
        <meta-data
            android:name="io.flutter.embedding.android.NormalTheme"
            android:resource="@style/NormalTheme" />


        <!-- ================================ -->
        <!-- APP LAUNCHER -->
        <!-- ================================ -->

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>


        <!-- ================================ -->
        <!-- OPEN PDF / DOC / DOCX / PPT / PPTX (MIME) -->
        <!-- ================================ -->

        <intent-filter>

            <action android:name="android.intent.action.VIEW" />

            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />

            <!-- PDF -->
            <data android:scheme="content" android:mimeType="application/pdf" />
            <data android:scheme="file" android:mimeType="application/pdf" />
            <data android:scheme="content" android:mimeType="application/x-pdf" />
            <data android:scheme="file" android:mimeType="application/x-pdf" />

            <!-- DOC -->
            <data android:scheme="content" android:mimeType="application/msword" />
            <data android:scheme="file" android:mimeType="application/msword" />
            <data android:scheme="content" android:mimeType="application/x-msword" />
            <data android:scheme="file" android:mimeType="application/x-msword" />

            <!-- DOCX -->
            <data android:scheme="content" android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
            <data android:scheme="file" android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />

            <!-- PPT -->
            <data android:scheme="content" android:mimeType="application/vnd.ms-powerpoint" />
            <data android:scheme="file" android:mimeType="application/vnd.ms-powerpoint" />
            <data android:scheme="content" android:mimeType="application/powerpoint" />
            <data android:scheme="file" android:mimeType="application/powerpoint" />
            <data android:scheme="content" android:mimeType="application/x-mspowerpoint" />
            <data android:scheme="file" android:mimeType="application/x-mspowerpoint" />

            <!-- PPTX -->
            <data android:scheme="content" android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
            <data android:scheme="file" android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />

        </intent-filter>

        <!-- ================================ -->
        <!-- OPEN VIA FILE EXTENSION PATTERN -->
        <!-- ================================ -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />

            <data android:scheme="file" />
            <data android:scheme="content" />
            <data android:host="*" />
            <data android:pathPattern=".*\\.pdf" />
            <data android:pathPattern=".*\\.PDF" />
            <data android:pathPattern=".*\\.docx" />
            <data android:pathPattern=".*\\.DOCX" />
            <data android:pathPattern=".*\\.doc" />
            <data android:pathPattern=".*\\.DOC" />
            <data android:pathPattern=".*\\.pptx" />
            <data android:pathPattern=".*\\.PPTX" />
            <data android:pathPattern=".*\\.ppt" />
            <data android:pathPattern=".*\\.PPT" />
        </intent-filter>

        <!-- ================================ -->
        <!-- SHARE / SEND DOCUMENT TO DOCVAULT -->
        <!-- ================================ -->
        <intent-filter>
            <action android:name="android.intent.action.SEND" />
            <category android:name="android.intent.category.DEFAULT" />

            <data android:mimeType="application/pdf" />
            <data android:mimeType="application/x-pdf" />
            <data android:mimeType="application/msword" />
            <data android:mimeType="application/x-msword" />
            <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
            <data android:mimeType="application/vnd.ms-powerpoint" />
            <data android:mimeType="application/powerpoint" />
            <data android:mimeType="application/x-mspowerpoint" />
            <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
        </intent-filter>


    </activity>


    <!-- Flutter embedding -->
    <meta-data
        android:name="flutterEmbedding"
        android:value="2" />

    <!-- ================================ -->
    <!-- GOOGLE CODE SCANNER: let Play services pre-download the -->
    <!-- scanner module at install time instead of on first scan. -->
    <!-- https://developers.google.com/ml-kit/vision/barcode-scanning/code-scanner -->
    <!-- ================================ -->
    <meta-data
        android:name="com.google.mlkit.vision.DEPENDENCIES"
        android:value="barcode_ui" />

</application>


<!-- Android package visibility -->
<queries>

    <intent>
        <action android:name="android.intent.action.PROCESS_TEXT" />

        <data android:mimeType="text/plain" />
    </intent>

    <!-- Needed so url_launcher can open scanned QR links (Android 11+ -->
    <!-- hides app visibility for other apps/browsers unless declared). -->
    <intent>
        <action android:name="android.intent.action.VIEW" />

        <data android:scheme="https" />
    </intent>

    <intent>
        <action android:name="android.intent.action.VIEW" />

        <data android:scheme="http" />
    </intent>

    <intent>
        <action android:name="android.intent.action.DIAL" />

        <data android:scheme="tel" />
    </intent>

    <intent>
        <action android:name="android.intent.action.VIEW" />

        <data android:scheme="tel" />
    </intent>

    <intent>
        <action android:name="android.intent.action.SENDTO" />

        <data android:scheme="mailto" />
    </intent>

    <intent>
        <action android:name="android.intent.action.VIEW" />

        <data android:scheme="mailto" />
    </intent>

    <intent>
        <action android:name="android.intent.action.SENDTO" />

        <data android:scheme="sms" />
    </intent>

    <intent>
        <action android:name="android.intent.action.VIEW" />

        <data android:scheme="sms" />
    </intent>

</queries>

</manifest>
